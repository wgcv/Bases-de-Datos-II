SELECT *
FROM EMPLOYEES;
SELECT *
FROM DEPARTMENTS ORDER BY DEPARTMENT_ID asc;
INSERT INTO DEPARTMENTS (DEPARTMENT_ID, DEPARTMENT_NAME,LOCATION_ID) VALUES (280, 'Sistema',1700);
INSERT INTO DEPARTMENTS VALUES (280, 'Sistema',NULL, NULL);
UPDATE DEPARTMENTS SET DEPARTMENT_NAME=INITCAP(DEPARTMENT_NAME);
DELETE FROM DEPARTMENTS WHERE DEPARTMENT_ID=280